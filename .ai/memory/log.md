# Session & Decision Log

> Append-only. One line per entry: `<date> — <agent> — <summary>`. When a completed
> feature's plan is archived here, add a `### <feature>` subheading above its entries.
> Keep this file under ~200 lines — prune the oldest entries into cold storage (e.g. a
> `log.archive.md`) once it grows past that, oldest first.

<!-- newest entries at the top -->
