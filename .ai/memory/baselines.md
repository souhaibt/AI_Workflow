# Configuration Items & Baselines (SUP.8)

> Owner: `configuration-manager`. A baseline is a named, frozen snapshot of a set of work
> products — only record one here once every item in it is `approved`/`baselined` per its
> own work product, with human sign-off. `.ai/safety/asil-manifest.md` is never part of a
> baseline change proposed by an agent — see `.claude/skills/safety-governance/SKILL.md`.

## Configuration Items

`<ID> — <work product/file> — <current version> — <owner role>`

## Baseline History

`<Baseline ID> — <date> — <contents (CI IDs/versions)> — <approved by (human)> — <git tag>`
