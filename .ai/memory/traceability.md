# Traceability Matrix

> Owner: shared — each subagent updates the columns it owns when it creates the linked
> artifact (see `.claude/skills/safety-governance/SKILL.md` for who may author what).
> `quality-assurance` audits completeness (SUP.1). Never delete a row; if an item is
> removed, set `Status` to `retired` and say why in a note, don't delete history.

| SW Req (`requirements.md`) | ASIL | Arch Element (`architecture.md`) | Unit/File | Unit Test | Integration Test | Qualification Test | Status |
|---|---|---|---|---|---|---|---|
| `<REQ-001>` | `<D>` | `<component/interface>` | `<path>` | `<test id>` | `<test id>` | `<test id>` | `open` |
