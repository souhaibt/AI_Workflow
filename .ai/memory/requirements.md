# Software Requirements (SWE.1)

> Owner: `requirements-engineer`. For ASIL A–D rows, the AI does not draft requirement
> text — it may only flag that one is needed and describe the gap; a human author fills
> the `Requirement`/`Verification criteria` cells and is recorded as `Author`. See
> `.claude/skills/safety-governance/SKILL.md`.

| ID | Requirement | Source (system/safety req) | ASIL | Verification criteria | Author | Reviewer | Status |
|---|---|---|---|---|---|---|---|
| `<REQ-001>` | `<...>` | `<SYS-REQ-.../FSR-.../TSR-...>` | `<QM\|A\|B\|C\|D>` | `<how it will be verified>` | `<name>` | `<name — must differ from Author if ASIL C/D>` | `draft` |

Status values: `draft`, `reviewed`, `approved`, `baselined`. Only a human may set
`approved`/`baselined`, on any row, QM or ASIL.
