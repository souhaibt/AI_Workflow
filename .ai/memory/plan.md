# Current Plan

> Owner: written by `planner` via the `plan-feature` skill; task status updated by whoever
> owns the task. Read this before starting work.
> When a feature ships, move this content into `.ai/memory/log.md` and reset this file.

## Feature

`<One-line description of the feature/change currently in flight. Empty if none.>`

## Acceptance Criteria

- `<criterion>`

## Task Breakdown

> One task per **vertical slice**, owned end to end — schema, logic, UI, and its tests in the
> same task. Splitting a slice across owners costs a cold context per handoff and buys nothing.

| #   | Task     | Owner    | Status | Notes |
| --- | -------- | -------- | ------ | ----- |
| 1   | `<task>` | `<agent>` | `todo` |       |

Status values: `todo`, `in-progress`, `blocked`, `review`, `done`.
